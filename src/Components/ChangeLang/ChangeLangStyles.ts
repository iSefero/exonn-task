export const styles = {
  wrapper: {
    textAlign: "center",
    gap: "5px"
  },

  hover: {
    background: "rgb(149 149 158 / 31%)!important"
  },

  divider: {
    height: "22px"
  },

  button: (color: string) => ({
    padding: "0px 5px",
    cursor: "pointer",
    fontSize: "15px",
    background: color,
  })
}